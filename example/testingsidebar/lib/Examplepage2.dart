import 'package:flutter/material.dart';

class Examplepage2 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("AppBar page 2"),
      ),
      body: Center(
        child: Text("Example content 2"),
      ),
    );
  }
}
